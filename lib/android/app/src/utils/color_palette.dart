import 'package:flutter/material.dart';

class ColorPalette {
  Color backgroundColor = const Color.fromRGBO(38, 35, 35, 1.0); //main background colour of AppBar and bottom AppBar
  Color middleBackgroundColor = const Color.fromRGBO(44, 40, 40, 1.0); //background colour in the middle of each page

  Color streamDetailsPageText = Colors.grey.shade300; //the colour of the texts in the StreamDetailsPage
}